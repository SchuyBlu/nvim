# Config

Simple barebones neovim config.

## Installation

The config is fairly simple to install. You'll need LazyGit installed.
You will also need to build `telescope-fzf-native`, as a limitation of the
built-in package manager.
